name: smart-assistant-env
description: Simple assistant environment

actions:
  - action_type: string
  - email_id: int

observation:
  inbox: list
  meetings: list
  time: string
  done: boolean

state:
  step_count: int
  completed_tasks: int